# StudyMind V1.3 — Deploy Ready
هذه النسخة تضيف اختبار اتصال حقيقي بالـOpenAI API من الـBackend.
لا تضع المفتاح داخل HTML/JavaScript. على الاستضافة أضف Secret باسم OPENAI_API_KEY.
بعد تشغيل السيرفر افتح الصفحة واضغط اختبار الاتصال.

مهم: هذه نسخة Deploy-Ready Prototype وليست نشرًا نهائيًا؛ ما زلنا نحتاج استضافة، قاعدة بيانات إنتاجية، HTTPS، rate limiting، مصادقة، ورفع PDF آمن قبل استقبال طلاب حقيقيين.
